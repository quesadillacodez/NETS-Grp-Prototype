node_modules
dist
tmp
.env
*.local
.DS_Store

# Playwright
/test-results
/playwright-report
/blob-report
/playwright/.cache

# Runtime server state (contains credential hashes and sessions)
/server/data/
/tmp/*.json

# Dev-only readable mirrors of the browser database
/database/snapshot.md
/database/tables/*.csv

# Generated presentation inspection traces (the deck and preview images remain tracked)
*.inspect.ndjson
